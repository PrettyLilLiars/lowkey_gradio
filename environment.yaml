name: lowkey
channels:
  - conda-forge
  - defaults
  - conda-forge/label/broken
dependencies:
  - python=3.10
  - pytorch
  - torchvision
  - tqdm
  - opencv
  - pytorch-gpu
prefix: /opt/conda/envs/lowkey
